apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mealie
  annotations:
spec:
  ingressClassName: traefic
  rules:
  - host: "mealie.rancher.localhost"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mealie-service
            port:
              number: 80